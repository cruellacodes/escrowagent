{
  "name": "agentvault",
  "version": "0.1.0",
  "description": "AgentVault — Trust & settlement layer for agent-to-agent transactions on Solana",
  "scripts": {
    "test": "anchor test",
    "test:local": "anchor test --skip-local-validator",
    "build": "anchor build",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "lint": "eslint tests/ sdk/"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/web3.js": "^1.95.0",
    "@solana/spl-token": "^0.4.9"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "chai": "^4.3.0",
    "mocha": "^10.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
