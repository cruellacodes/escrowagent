{
  "name": "escrowagent",
  "version": "0.2.0",
  "description": "EscrowAgent — Trust & settlement layer for agent-to-agent transactions on Solana and Base",
  "scripts": {
    "test": "anchor test",
    "test:local": "anchor test --skip-local-validator",
    "test:base": "cd contracts && forge test -vv",
    "build": "anchor build",
    "build:base": "cd contracts && forge build",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "deploy:base-sepolia": "cd contracts && forge script script/DeployUpgradeable.s.sol --rpc-url $BASE_RPC_URL --broadcast --verify",
    "deploy:base-mainnet": "cd contracts && forge script script/DeployUpgradeable.s.sol --rpc-url $BASE_RPC_URL --broadcast --verify",
    "lint": "eslint tests/ sdk/"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/web3.js": "^1.95.0",
    "@solana/spl-token": "^0.4.9"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "chai": "^4.3.0",
    "mocha": "^10.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
